import { FilterUrlPipe } from './filter-url.pipe';

describe('FilterUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
